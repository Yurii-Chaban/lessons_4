<%= form_with model: @field, local: true do |form| %>

    <% if @field.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@field.errors.count, "error") %> prohibited
            this field from being saved:
          </h2>
          <ul>
            <% @field.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= form.label :field_categorye %><br>
      <%= form.text_field :field_categorye %>
    </p>

    <p>
      <%= form.label :field %><br>
      <%= form.text_area :field %>
    </p>

    <p>
      <%= form.label :answer %><br>
      <%= form.text_area :answer %>
    </p>

    <p>
      <%= form.submit %>
    </p>

<% end %>